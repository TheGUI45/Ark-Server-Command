Place the provided dinosaur PNG here named dino.png (square, preferably 1024x1024 or 512x512). Then run:

npm install
npm run icons
npm run dist

This will generate build/icon.ico, icon.icns, and icon.png for Electron packaging (Windows, macOS, Linux).
